"""View components for the integrated app. Phase 5 - not implemented yet.

The UI framework is not chosen yet, so nothing here should import one. Keep the
framework-specific code in `app/` and keep this package to framework-agnostic
pieces: state models, formatting, and the read-only views of robot state that any
frontend needs.

See docs/05-app.md.
"""
