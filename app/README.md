# 統合アプリケーション（フェーズ 5・未着手）

フェーズ 1〜4 を 1 つのアプリケーションにまとめる場所です。

UI フレームワークが未決定のため、まだ実装はありません。方針は [../docs/05-app.md](../docs/05-app.md) に記載しています。

フレームワーク依存のコードはこのディレクトリに置き、フレームワークに依存しない状態モデルや表示用の整形は `src/so101/ui/` に置きます。こう分けておけば、フレームワークを乗り換えても `src/so101/` 側は影響を受けません。
