[project]
name = "so101"
version = "0.1.0"
description = "SO-101 exhibition demo: hardware bring-up, camera, block detection, and a touch-screen pick-and-place"
requires-python = ">=3.11,<3.12"
dependencies = [
    "lerobot[feetech]",
    "pyserial",
    # DirectShow device names, so a camera can be identified by name instead of by
    # an index Windows is free to reshuffle.
    "pygrabber; sys_platform == 'win32'",
    "ultralytics",
    # Named directly so [tool.uv.sources] applies: uv only honours source overrides
    # for a project's own dependencies, not for ones pulled in transitively.
    "torch",
    "torchvision",
]

# LeRobot pulls the CPU build of torch. This machine has an RTX 4060, and
# training the block detector is impractical without it.
[tool.uv.sources]
torch = [{ index = "pytorch-cu128" }]
torchvision = [{ index = "pytorch-cu128" }]

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[dependency-groups]
dev = ["pytest"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/so101"]
